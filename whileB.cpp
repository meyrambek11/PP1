#include <iostream>
#include <cmath>

using namespace std;

int main (){
float a, sum=0;
   float n;
    cin >> n;
   /* while (n>a){
        a=a*2;
        sum=sum+a;
       }
cout << sum;

    */
a=pow(2,n);
cout << a << endl;
    return 0;
    
        
    
}