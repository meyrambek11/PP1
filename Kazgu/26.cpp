#include <bits/stdc++.h>
using namespace std;
int main(){
    int x,y,z;
    cin >> x >> y >> z;
    if(x>0 && y > 0 && z > 0){
        cout << "Yes, We can build trangle" << endl;
    }
    else
    cout << "No, We can not build trangle" << endl; 

}