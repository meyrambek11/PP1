#include <iostream>
using namespace std;
int main(){
    int x,k;
    cin >> x;
    int a[x];
    for(int i=1;i<=x;i++){
        cin >> a[i];
    }
    for(int i=1;i<=x;i++){
       if(a[i] > 0){
           a[i]=a[i]*1;
       }
       else if(a[i] < 0){
           a[i]=a[i]*(-1);
       }
    }
    for(int i=1;i<=x;i++){
        k = 
    }
    

}